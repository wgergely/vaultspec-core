# Search Guide Retired

This page is retired. It previously described CLI `search` and `index`
features that have been removed, and there is no shipped CLI `search` or
`index` surface.

- Use the live MCP query guide instead:
  [vault-query-guide.md](./vault-query-guide.md)
