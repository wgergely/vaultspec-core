# Example hook: auto-verify vault after document creation.
#
# This hook runs `vault.py audit --verify` whenever a new document
# is created via `vault.py create`. Remove the `.example` suffix
# (or copy this file) to activate it.
#
# Available events:
#   vault.document.created   — after vault.py create
#   vault.document.modified  — after vault doc edits
#   vault.index.updated      — after vault.py index
#   config.synced            — after cli.py sync-all
#   audit.completed          — after vault.py audit
#
# Available action types:
#   shell  — run a shell command
#   agent  — dispatch a sub-agent via ACP
#
# Context variables (available in {placeholders}):
#   {path}     — path to the affected document
#   {root}     — workspace root directory
#   {event}    — the event name

event: vault.document.created
enabled: false  # set to true to activate
actions:
  - type: shell
    command: "python .vaultspec/lib/scripts/vault.py audit --verify"
