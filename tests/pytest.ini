[pytest]
asyncio_mode = auto
markers =
    integration: requires CLI tools and network (deselect: -m "not integration")
    gemini: requires Gemini CLI installed
    claude: requires Claude CLI installed
    slow: tests requiring full 213-doc corpus (deselect: -m "not slow")
    timeout: timeout in seconds for individual tests (requires pytest-timeout)
